---
version: '3'

services:
  overseerr:
    image: sctx/overseerr:latest
    container_name: minerva-overseerr
    environment:
      - LOG_LEVEL=debug
      - TZ=America/Los_Angeles
      - PORT=5055 #optional
    ports:
      - 5055:5055
    volumes:
      - /volume1/docker/overseerr/config:/app/config
    restart: unless-stopped
